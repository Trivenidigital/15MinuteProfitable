Finishing current task:

1. Run full test suite: `pytest tests/ -v`
2. Run linter and fix any issues: `ruff check src/ tests/`
3. Run type checker: `mypy src/`
4. Update tasks/todo.md - move task to "Completed"
5. Generate a conventional commit message
6. Show me `git diff main` for final review
7. Wait for my approval before committing
